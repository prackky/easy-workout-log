{
  "verbose": true,
  "src": {
    "beforeStartCommand": "CI=true npm test; json -I -f package-lock.json -e 'this.version=\"${version}\"'",
    "afterReleaseCommand": "echo Successfully released ${version}"
  }
}

